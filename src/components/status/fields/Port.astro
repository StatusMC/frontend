---
import type { AnyStatusAnswer } from "../interfaces";
import Base from "./Base.astro";

export type Props = AnyStatusAnswer;

const { status }: Props = Astro.props;
---

<Base nameValue="Port">
	{status.address.port.toString()}
</Base>
