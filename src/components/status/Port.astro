---
import type { AnyStatusAnswer } from "~/components/status/interfaces";
import Base from "~/components/status/Base.astro";

export type Props = AnyStatusAnswer;

const { status }: Props = Astro.props;
---

<Base nameValue="Port">
	{status.address.port.toString()}
</Base>
