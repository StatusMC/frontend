---
import type { JavaStatusAnswer, BedrockStatusAnswer } from "~/components/status/interfaces";
import Base from "~/components/status/Base.astro";

export type Props = { status: JavaStatusAnswer | BedrockStatusAnswer };

const { status }: Props = Astro.props;
---

<Base nameValue="Version protocol">{status.version.protocol}</Base>
