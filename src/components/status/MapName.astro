---
import type { BedrockStatusAnswer, QueryAnswer } from "~/components/status/interfaces";
import Base from "~/components/status/Base.astro";

export type Props = { status: BedrockStatusAnswer | QueryAnswer };

const { status }: Props = Astro.props;
---

{status.map_name ? <Base nameValue="Map Name">{status.map_name}</Base> : null}
